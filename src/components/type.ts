export interface responseData {
    code: number
    message: string
    ok: boolean
    index: number
}

export interface ttt {
    //品号
    strlen: number
    label: string
    prop: string
    width: number
    vhide: boolean
    orderby: boolean
    isShow: boolean
    sort: number
}